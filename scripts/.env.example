# Copy this file to .env and customise. All keys are optional.

# Where to write the results CSV. Absolute or relative; relative paths resolve
# against the current working directory. The CLI flag --output overrides this.
# Default: ./results.csv (in whatever directory you run main.py from).
#RESULTS_CSV=./results.csv
